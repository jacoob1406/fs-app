import React from 'react';
import Loader from 'react-loader-spinner';

function LoaderIndicator() {
  return <Loader type='Circles' color='#ffc703' height={100} width={100} />;
}

export default LoaderIndicator;
